---
import ListCard from "../../components/post/list/ListCard.astro";
import DefaultLayout from "../../layouts/DefaultLayout.astro"

const list = ["item","minecraft"]

---

<DefaultLayout title="hi" description="post">
    {list.map(x => (
        <ListCard title={x} url={"/post/minecraft/1"} tag={"a,b,c"}/>
    ))}

</DefaultLayout>